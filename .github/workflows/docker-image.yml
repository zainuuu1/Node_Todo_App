name: Docker Image Publish to DockerHub

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - name: Listing files
      run: ls -ll
    - name: Build the Docker image
      run: |
        docker build . -t ${{ VARS_DOCKERUSERNAME }}/my-pipelinee-test-image
        docker image ls

    - name: Pushing Image to DockerHub
      run: |
        echo "${{ SECRETS_DOCKER_PAT }}" | docker login -u ${{ VARS_DOCKERUSERNAME }} --password-stdin
        docker push ${{ VARS_DOCKERUSERNAME }}/my-pipelinee-test-image
        
